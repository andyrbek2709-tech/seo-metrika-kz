---
name: product-discovery
description: |
  Навык-оркестратор: превращает поисковый спрос в продуктовые идеи. Полный цикл — сгенерировать запросы и их варианты по теме, снять реальный спрос (Wordstat/Google Trends), кластеризовать в темы рынка, проанализировать что аудитория РЕАЛЬНО ищет и хочет, и спланировать приложения/инструменты на Claude. Заточен под домен «проектирование проектной документации + прохождение Госэкспертизы» (KZ/СНГ), но применим к любой нише. Используй навык ОБЯЗАТЕЛЬНО, когда пользователь:
  - просит проанализировать рынок/спрос, «что реально ищут и хотят» проектировщики, инженеры, эксперты Госэкспертизы, любая аудитория
  - хочет на основе топ-запросов придумать/спланировать программы, приложения, инструменты, продукты (особенно на Claude/AI)
  - говорит «парсить что ищут», «собрать спрос и сделать выводы», discovery-исследование, product discovery, поиск идей продукта по запросам
  - просит сгенерировать запросы и варианты запросов под съём спроса, расширить семантику по намерениям
  - хочет отчёт с идеями продуктов, ранжированный по спросу и боли
  Опирается на парные навыки wordstat (Яндекс) и gtrends/google-kwp (Google) для съёма данных. Даже если пользователь не назвал «навык» — при разговоре про спрос→продукт запускай этот навык.
---

# Навык: Product Discovery (спрос → идеи продуктов)

## Назначение

Соединяет четыре шага в один воспроизводимый воркфлоу:

1. **Генерация запросов** — из коротких «семян» строим веер формулировок по типам намерения.
2. **Съём спроса** — сколько и что реально ищут (через навыки `wordstat`/`gtrends`/`google-kwp`).
3. **Анализ рынка** — кластеризуем в темы, читаем что аудитория **реально хочет** (jobs-to-be-done и боль).
4. **Планирование** — ранжированный отчёт идей приложений/инструментов на Claude.

Базовый домен — **проектирование ПД + прохождение Госэкспертизы** (KZ). Аудитория:
проектировщики, ГИПы, эксперты Госэкспертизы. Навык применим и к другой нише — меняются
семена и словарь тем.

**Принцип WAT** (Workflow + AI Thinking): механику (раскрытие шаблонов, парсинг, суммы,
ранжирование) делают скрипты; смысловые решения (какие семена, что значат цифры, какие
продукты строить) — в чате. Скрипт даёт каркас, аналитик даёт смысл.

## Инструменты

| Скрипт | Что делает | Когда |
|---|---|---|
| `scripts/expand_seeds.py` | семена → кандидаты-запросы по намерениям | шаг 1 |
| `scripts/cluster_demand.py` | .xlsx спроса → темы рынка с частотой и топами | шаг 3 |
| `assets/report_template.md` | каркас итогового отчёта с идеями | шаг 4 |

Намерения в `expand_seeds.py`: `info` (что это/состав), `howto` (как пройти/сделать),
`cost` (цена/сроки), `service` (заказать/услуги), `problem` (ошибки/замечания/отказ —
**самый ценный сигнал для продукта**), `geo` (рк/казахстан/алматы/астана).

## Рабочий процесс

### Шаг 1 — Сгенерировать запросы и варианты

Старт — `seeds_pd_gosexpertiza.txt` (в `wordstat-skill/`; темы: разработка ПД + Госэкспертиза).
Не хватает — допиши семена под конкретный вопрос discovery.

```bash
# полный веер кандидатов (для API-съёма или широкого охвата)
python scripts/expand_seeds.py --seeds ../wordstat-skill/seeds_pd_gosexpertiza.txt \
  --out candidates.txt

# узко: только коммерческий спрос и боль + гео РК (для ручной выгрузки — меньше фраз)
python scripts/expand_seeds.py --seeds ../wordstat-skill/seeds_pd_gosexpertiza.txt \
  --intents cost service problem --geo --group --out candidates.txt
```

Полный веер даёт сотни кандидатов — это нормально для API, но для **ручной** веб-выгрузки
из Wordstat отбери в чате 30–60 самых осмысленных строк (грамматически кривые комбинации
вроде «ошибки проектная документация» оставь — Wordstat нормализует словоформы и сам
вернёт 0 по мусорным). Помни: лишний кандидат дешевле пропущенного спроса.

### Шаг 2 — Снять спрос (делегируй парным навыкам)

Этот навык **не ходит в API сам** — он оркеструет. Источник выбирай по задаче:

- **KZ-аудитория, объём** → Google: навык `gtrends` (относит., без аккаунта) или
  `google-kwp` (абсолют., нужен аккаунт Ads).
- **Точные числа, проф-хвост** → Яндекс: навык `wordstat` (команда `import` —
  ручная веб-выгрузка, регион Казахстан; рабочий путь без API/карты).

Результат каждого — `.xlsx` с единой схемой (`запрос, ср_частота_мес, …`). Для
кросс-источникового сравнения прогоняй один и тот же список кандидатов в обоих.

Если данных ещё нет — честно скажи об этом и предложи сначала выгрузить (через `wordstat`),
не выдумывай цифры. Можно показать каркас анализа на пустом шаблоне.

### Шаг 3 — Кластеризовать в темы рынка

```bash
python scripts/cluster_demand.py --xlsx ../kz_yandex.xlsx --out themes.xlsx
# несколько источников сразу:
python scripts/cluster_demand.py --xlsx ../kz_yandex.xlsx ../kz_google.xlsx --top 8
```

Даёт темы с суммарной частотой, долей и топ-запросами + лист «Прочее» (не попавшее в
темы — сырьё для новых тем, смотри глазами). Словарь тем по умолчанию заточен под домен;
свой передаётся через `--themes theme.json` (тема → список ключей-подстрок).

### Шаг 4 — Анализ + идеи продуктов (в чате, по шаблону)

Скопируй `assets/report_template.md` и заполни поверх данных. Ядро навыка — здесь, и это
работа мышления, а не скрипта:

- **Что реально хотят** — за каждой крупной темой назови задачу (JTBD) и боль. Запросы
  с намерением `problem` («замечания», «отказ», «ошибки», «доработка») — прямой крик о
  проблеме, которую продукт может снять. Это ценнее голого объёма.
- **Идеи продуктов на Claude** — каждую привязывай к запросам, которые её оправдывают.
  Ранжируй по: спрос × боль × fit-с-Claude (работа с текстом/документами/нормами —
  сильная сторона LLM) ÷ сложность MVP. Для топ-1–2 — абзац про MVP (вход/выход, где
  Claude в центре, первый экран).
- **Честность** — спрос ≠ готовность платить. В «следующих шагах» предложи валидацию
  топ-идей разговором с 3–5 проектировщиками/экспертами.

## Связь с проектом

Парные навыки-источники: `../wordstat-skill`, `../gtrends-skill`, `../google-kwp-skill`.
Семена: `../wordstat-skill/seeds_pd_gosexpertiza.txt`. Шаблон сравнения источников:
`../АНАЛИЗ_KZ_Google_vs_Yandex.md` (Google vs Яндекс) — дополняет отчёт этого навыка
(спрос → продукт). Гео KZ = GeoID 159 (Wordstat) / 2398 (Google).
