# Анализ: что и сколько ищут в Google KZ-инженеры, проектировщики и эксперты Госэкспертизы

**Статус (2026-05-30):** 🟡 аналитический каркас готов, ждёт измеренных данных.
Качественный вывод и гипотезы по аренам сформулированы ниже и опираются на известную
структуру рынка KZ. Числовые ячейки помечены `⧖ ждёт выгрузки` — их заполняем после
прогона `google-kwp-skill`, **не подставляя оценочных чисел вручную**.

Ближайший деливерабл — **абсолютные объёмы спроса по Google для KZ** через
`google-kwp-skill` на бэкенде **DataForSEO** (без аккаунта Google Ads и без карты —
нужен лишь логин/пароль DataForSEO): прогнать арены засева `seeds_discovery.txt`
(`--geo 2398 --lang-code ru`) → `kz_google.xlsx`, затем перенести числа в таблицы ниже.

Метрика — **среднемесячный объём поиска** (search volume) по KZ, плюс конкуренция и
ставки Keyword Planner. Почему Google: он занимает ~95% поискового рынка Казахстана,
поэтому именно он отражает реальный спрос KZ-аудитории профессионалов.

---

## Краткий вывод (гипотеза до прогона)

Для аудитории Казахстана — инженеров-проектировщиков, ГИПов и экспертов Госэкспертизы —
**Google — основной и достаточный источник замера спроса**: ~95% поиска в стране, аудитория
профессионалов ищет преимущественно по-русски именно в нём.

Что мы хотим понять из данных:
- **где объём** — какие арены и темы крупнее по числу запросов;
- **где боль** — запросы намерения «проблема» (`замечания`, `отказ`, `ошибки`, `доработка`)
  как прямой сигнал продуктовой возможности;
- **где деньги** — высокая конкуренция/ставки часто = платёжеспособный спрос в нише.

**Рекомендация:** продукт и контент вести под **Google-KZ**; абсолютные числа снимать
через DataForSEO, проблемные разделы ПД (МОПБ, конструктив, сметы) разбирать отдельным
засевом `seeds_problem_sections.txt`.

> Вывод нужно подтвердить цифрами — см. таблицы ниже.

---

## Гипотезы по аренам (до данных)

Засев — три арены (`seeds_discovery.txt`). Ожидаемое поведение по Google (KZ):

| Арена | Что в ней | Ожидание по Google (KZ) | Почему |
|---|---|---|---|
| **A — экспертиза / нормоконтроль** | `госэкспертиза`, `экспертиза проектной документации`, `нормоконтроль`, `замечания экспертизы`, `комплектность ПД` | средний объём, **высокая локальность KZ** | Узкий локальный B2B-домен; терминология и регламенты KZ-специфичны. |
| **B — широкий инженерный / проектный** | `проектирование пожарной сигнализации`, `оформление чертежей по ГОСТ`, `сметная документация`, `BIM проектирование`, `Revit обучение` | **самый крупный объём** | Массовый русскоязычный рынок; ГОСТ/BIM/Revit — общие для СНГ. |
| **C — AI-автоматизация (продукт)** | `нейросеть для документов`, `автоматизация документооборота`, `проверка документов нейросеть`, `ии для инженеров` | растущий, но пока **тонкий** объём | Спрос ранний/формирующийся; важнее динамика и состав, чем абсолют. |

**Продуктовый смысл:** арена **C — пересечение боли (A) и масштаба (B)** и есть целевая ниша
продукта на Claude. A даёт локальную экспертную глубину, B — объём аудитории, C — место,
где автоматизация закрывает реальную работу.

---

## Объём спроса по аренам — Google (KZ), DataForSEO

Среднемесячный объём поиска. Источник — `kz_google.xlsx`.

| Арена | Топ-запрос | Объём топа | Сумма объёма арены | Покрыто запросов | Комментарий |
|---|---|---|---|---|---|
| A — экспертиза/нормоконтроль | ⧖ ждёт выгрузки | ⧖ | ⧖ | ⧖ | |
| B — широкий инженерный | ⧖ ждёт выгрузки | ⧖ | ⧖ | ⧖ | |
| C — AI-автоматизация | ⧖ ждёт выгрузки | ⧖ | ⧖ | ⧖ | |

**Вердикт по объёму (где спрос выше):** ⧖ ждёт выгрузки

---

## Боль по проблемным разделам ПД — Google (KZ)

Отдельный засев `seeds_problem_sections.txt` (МОПБ, конструктив, сметы, энергоэффективность).
Здесь ищем не объём «вообще», а где чаще всего замечания/отказы — там острее спрос на помощь.

| Раздел ПД | Объём спроса | Запросы-боль (отказ/замечания/как пройти) | Claude-fit |
|---|---|---|---|
| МОПБ (пожарная безопасность) | ⧖ | ⧖ | высокий |
| Конструктив / расчёты (КР) | ⧖ | ⧖ | высокий (обвязка, не МКЭ) |
| Смета | ⧖ | ⧖ | высокий |
| Энергоэффективность / ООС / инсоляция | ⧖ | ⧖ | средний-высокий |

---

## Методика

1. Засев `seeds_discovery.txt` (3 арены) снимается по Google KZ через DataForSEO:
   `gkp_client.py ideas --phrases-file seeds_discovery.txt --geo 2398 --lang-code ru --out kz_google.xlsx`
   (или `volume` — только объёмы по точным фразам; бесплатная альтернатива — ручной `import` CSV).
2. Проблемные разделы — отдельным прогоном `seeds_problem_sections.txt` (там же намерение
   «проблема» через `product-discovery-skill/scripts/expand_seeds.py`).
3. Кластеризация в темы рынка — `product-discovery-skill/scripts/cluster_demand.py
   --xlsx kz_google.xlsx --out themes.xlsx`.
4. Метрики сравнения: суммарный объём, число покрытых запросов, топ-запросы, запросы-боль,
   уровень конкуренции/ставок по теме.

---

## Gap-запросы и аномалии (заполнить после прогона)

- **Высокий объём + слабая конкуренция:** ⧖ — недозакрытые ниши для контента/продукта.
- **Высокая конкуренция/ставки:** ⧖ — где есть платёжеспособный спрос.
- **Запросы-боль (intent=problem):** ⧖ — прямые продуктовые возможности.

---

## Итоговый вердикт

| Вопрос | Гипотеза (до данных) | Подтверждено цифрами |
|---|---|---|
| Где больше объём по KZ | **Арена B** (широкий инженерный) | ⧖ ждёт выгрузки |
| Где острее боль (продукт) | **Проблемные разделы ПД** (МОПБ, конструктив, сметы) | ⧖ ждёт выгрузки |
| Канал продукта/контента | **Google-KZ** (~95% рынка) | ⧖ ждёт выгрузки |

**Следующий шаг:** положить `~/dataforseo.yaml`, прогнать `seeds_discovery.txt` и
`seeds_problem_sections.txt` по KZ (`--geo 2398 --lang-code ru`), заполнить ячейки `⧖`
и снять статус с 🟡 на ✅.
