# Анализ: где больше ищут KZ-инженеры, проектировщики и эксперты Госэкспертизы — Google или Яндекс

**Статус (2026-05-30):** 🟡 аналитический каркас готов, ждёт измеренных данных.
Качественный вывод и гипотезы по аренам сформулированы ниже и опираются на известную
структуру рынка KZ. Числовые ячейки помечены `⧖ ждёт выгрузки` — их заполняем после
прогона навыков, **не подставляя оценочных чисел вручную**.

Ближайший деливерабл — **относительная картина спроса по Google для KZ** через Google
Trends (бесплатно, без аккаунта/карты): выгрузить батчи по аренам засева
`seeds_discovery.txt` (регион = Казахстан, период 12 мес, анкор «проектная документация»),
прогнать `gtrends-skill/scripts/trends_import.py` → `kz_trends.xlsx`, затем перенести числа
в таблицу относительного интереса.

Метрика Trends — **относительный интерес 0–100**, не «N запросов/мес». Абсолютные числа
Google берутся через `google-kwp-skill` на бэкенде **DataForSEO** (объёмы Keyword Planner
по KZ **без аккаунта Google Ads и без карты** — нужен лишь логин/пароль DataForSEO).
Колонка **Яндекс** снимается через `wordstat-skill import` ручной веб-выгрузкой
(абсолютные показы + разбивка по регионам KZ); API-путь Директа заблокирован.

---

## Краткий вывод (гипотеза до прогона)

Для аудитории Казахстана — инженеров-проектировщиков, ГИПов и экспертов Госэкспертизы —
**по абсолютному объёму запросов почти наверняка лидирует Google**.

Причины:
- Google занимает ~95% поискового рынка Казахстана; Яндекс в KZ — нишевый игрок.
- KZ-аудитория профессионалов ищет в Google, преимущественно по-русски.
- Яндекс.Wordstat по гео=KZ покажет заметно меньшие показы, чем реальный спрос в Google.

**Но это не значит, что Яндекс бесполезен.** Его роль:
- даёт **точные абсолютные числа** частотности (Google без spend — только диапазоны);
- богаче по **длинному хвосту** профессиональных русскоязычных запросов;
- служит **калибровкой и источником расширения семантики**, замеряя весь русскоязычный
  рынок (в основном РФ), а не только KZ.

**Рекомендация:** продукт и контент вести под **Google-KZ**, а Яндекс использовать как
исследовательский инструмент — для полноты ядра и оценки рынка РФ при будущей экспансии.

> Вывод нужно подтвердить цифрами — см. таблицы ниже.

---

## Гипотезы по аренам (до данных)

Засев — три арены (`seeds_discovery.txt`). Ожидаемое поведение каждого источника:

| Арена | Что в ней | Ожидание по Google (KZ) | Ожидание по Яндексу | Почему |
|---|---|---|---|---|
| **A — экспертиза / нормоконтроль** | `госэкспертиза`, `экспертиза проектной документации`, `нормоконтроль`, `замечания экспертизы`, `комплектность ПД` | средний объём, но **высокая локальность KZ** | низкий по гео=KZ | Узкий локальный B2B-домен; терминология и регламенты KZ-специфичны → ищут в Google. |
| **B — широкий инженерный / проектный** | `проектирование пожарной сигнализации`, `оформление чертежей по ГОСТ`, `сметная документация`, `BIM проектирование`, `Revit обучение` | **самый крупный объём** | заметный объём (но в основном РФ-трафик) | Массовый русскоязычный рынок; ГОСТ/BIM/Revit — общие для СНГ → даёт хвост и в Яндексе. |
| **C — AI-автоматизация (продукт)** | `нейросеть для документов`, `автоматизация документооборота`, `проверка документов нейросеть`, `ии для инженеров` | растущий, но пока **тонкий** объём | тонкий | Спрос ранний/формирующийся; важнее динамика (Trends), чем абсолют. |

**Продуктовый смысл:** арена **C — пересечение боли (A) и масштаба (B)** и есть целевая ниша
продукта на Claude. A даёт локальную экспертную глубину, B — объём аудитории, C — место,
где автоматизация закрывает реальную работу.

---

## Относительный интерес по аренам — Google Trends, KZ

Шкала 0–100, нормировано по анкору «проектная документация». Источник — `kz_trends.xlsx`.

| Арена | Топ-фраза | Интерес топа | Сумма интереса арены | Комментарий |
|---|---|---|---|---|
| A — экспертиза/нормоконтроль | ⧖ ждёт выгрузки | ⧖ | ⧖ | |
| B — широкий инженерный | ⧖ ждёт выгрузки | ⧖ | ⧖ | |
| C — AI-автоматизация | ⧖ ждёт выгрузки | ⧖ | ⧖ | |

**Вердикт по Trends (где интерес выше):** ⧖ ждёт выгрузки

---

## Методика

1. Один и тот же засев `seeds_discovery.txt` (3 арены) снимается в обоих источниках на KZ:
   - **Google Trends** (бесплатно): веб-выгрузка батчей `A.csv/B.csv/C.csv` с общим анкором
     «проектная документация» → `trends_import.py --csv A.csv B.csv C.csv --anchor "проектная документация" --out kz_trends.xlsx`.
   - **Яндекс Wordstat** (бесплатно): веб-выгрузка `phrase.xlsx` (регион Казахстан, вкладка
     «Топы запросов») → `wordstat_client.py import --file phrase.xlsx --out kz_yandex.xlsx`.
   - **Google Keyword Planner** через DataForSEO (без карты): `gkp_client.py ideas
     --phrases-file seeds_discovery.txt --geo 2398 --lang-code ru --out kz_google.xlsx` —
     абсолютные числа Google. Бесплатная альтернатива — ручной `import` CSV из веб-Планировщика.
2. Wordstat и Keyword Planner пишут **идентичные колонки** → сливаются построчно и по аренам.
   Google Trends — отдельная шкала относительного интереса.
3. Метрики сравнения: суммарная частотность, число покрытых запросов, топ-запросы,
   gap-запросы (есть в одном источнике — нет в другом).

> Важно: Trends и Wordstat сопоставимы только по **структуре** (какие темы крупнее
> относительно других), а не по абсолюту. Прямое сравнение «объёма Google против объёма
> Яндекса» в числах появится лишь с Keyword Planner.

---

## Результаты по аренам (заполнить после прогона)

### Арена A — экспертиза / нормоконтроль (ядро KZ)
| Метрика | Google (KZ) | Яндекс (KZ) | Комментарий |
|---|---|---|---|
| Суммарная ср. частота/мес | ⧖ | ⧖ | |
| Покрыто запросов (идей) | ⧖ | ⧖ | |
| Топ-3 запроса | ⧖ | ⧖ | |

### Арена B — широкий инженерный / проектный рынок
| Метрика | Google (KZ) | Яндекс (KZ) | Комментарий |
|---|---|---|---|
| Суммарная ср. частота/мес | ⧖ | ⧖ | |
| Покрыто запросов (идей) | ⧖ | ⧖ | |
| Топ-3 запроса | ⧖ | ⧖ | |

### Арена C — AI-автоматизация документов (пересечение, продукт)
| Метрика | Google (KZ) | Яндекс (KZ) | Комментарий |
|---|---|---|---|
| Суммарная ср. частота/мес | ⧖ | ⧖ | |
| Покрыто запросов (идей) | ⧖ | ⧖ | |
| Топ-3 запроса | ⧖ | ⧖ | |

---

## Gap-запросы (заполнить после прогона)

- **Только в Google (нет в Яндексе):** ⧖ — кандидаты на KZ-локальный контент.
- **Только в Яндексе (нет в Google):** ⧖ — кандидаты в расширение ядра (РФ-хвост).

---

## Итоговый вердикт

| Вопрос | Гипотеза (до данных) | Подтверждено цифрами |
|---|---|---|
| Где больше объём по KZ | **Google** (~95% рынка KZ) | ⧖ ждёт выгрузки |
| Где богаче проф-хвост | **Яндекс** (весь рус. рынок, в осн. РФ) | ⧖ ждёт выгрузки |
| Канал продукта/контента | **Google-KZ**, Яндекс — как research | ⧖ ждёт выгрузки |

**Следующий шаг:** прогнать Google Trends (KZ, 12 мес, анкор «проектная документация») по
трём батчам засева и Wordstat (регион Казахстан) — заполнить ячейки `⧖` и снять статус с
🟡 на ✅.
